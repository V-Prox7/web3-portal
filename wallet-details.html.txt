<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Wallet Details - Node Validator Hub</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #0f0f2e 0%, #1a1a3f 100%);
      color: #e0e0e0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1.5rem;
    }

    .card {
      background: rgba(30, 30, 60, 0.9);
      border-radius: 16px;
      padding: 2rem;
      max-width: 420px;
      width: 100%;
      border: 1px solid rgba(0, 212, 255, 0.4);
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
    }

    .card h1 {
      font-size: 1.6rem;
      margin-bottom: 0.5rem;
      color: #fff;
    }

    .card p {
      font-size: 0.95rem;
      color: #b0b0b0;
      margin-bottom: 1.5rem;
    }

    .field {
      margin-bottom: 1rem;
    }

    .field label {
      display: block;
      font-size: 0.9rem;
      margin-bottom: 0.4rem;
      color: #e0e0e0;
    }

    .field input,
    .field select,
    .field textarea {
      width: 100%;
      padding: 0.7rem 0.8rem;
      border-radius: 8px;
      border: 1px solid rgba(0, 212, 255, 0.4);
      background: rgba(10, 10, 30, 0.9);
      color: #e0e0e0;
      font-size: 0.95rem;
    }

    .field textarea {
      resize: vertical;
      min-height: 70px;
    }

    .helper {
      font-size: 0.8rem;
      color: #888;
      margin-top: 0.25rem;
    }

    .btn-row {
      display: flex;
      justify-content: space-between;
      gap: 0.75rem;
      margin-top: 1.5rem;
    }

    .btn {
      flex: 1;
      padding: 0.8rem 1rem;
      border-radius: 8px;
      border: none;
      cursor: pointer;
      font-size: 0.95rem;
      font-weight: 600;
      transition: all 0.25s ease;
    }

    .btn-primary {
      background: #00d4ff;
      color: #0b1020;
    }

    .btn-primary:hover {
      background: #30e0ff;
      transform: translateY(-2px);
    }

    .btn-secondary {
      background: transparent;
      color: #00d4ff;
      border: 1px solid rgba(0, 212, 255, 0.6);
    }

    .btn-secondary:hover {
      background: rgba(0, 212, 255, 0.1);
      transform: translateY(-2px);
    }

    .badge {
      display: inline-block;
      font-size: 0.75rem;
      padding: 0.2rem 0.6rem;
      border-radius: 999px;
      background: rgba(0, 212, 255, 0.15);
      color: #00d4ff;
      margin-bottom: 0.75rem;
    }

    @media (max-width: 480px) {
      .card {
        padding: 1.5rem;
      }
      .btn-row {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>
  <div class="card">
    <span class="badge">Step 2 of 2</span>
    <h1>Validator Wallet Details</h1>
    <p>Fill in the basic details for the wallet you want to use as your validator account.</p>

    <form>
      <div class="field">
        <label for="network">Network</label>
        <select id="network" name="network">
          <option value="">Select network</option>
          <option value="ethereum">Ethereum</option>
          <option value="bnb">BNB Chain</option>
          <option value="polygon">Polygon</option>
          <option value="solana">Solana</option>
        </select>
      </div>

      <div class="field">
        <label for="walletAddress">Wallet address</label>
        <input
          type="text"
          id="walletAddress"
          name="walletAddress"
          placeholder="0x..."
        />
        <div class="helper">Do not share your private key or seed phrase.</div>
      </div>

      <div class="field">
        <label for="label">Label (optional)</label>
        <input
          type="text"
          id="label"
          name="label"
          placeholder="e.g. Main validator wallet"
        />
      </div>

      <div class="field">
        <label for="notes">Notes (optional)</label>
        <textarea
          id="notes"
          name="notes"
          placeholder="Any extra information about this validator setup..."
        ></textarea>
      </div>

      <div class="btn-row">
        <button type="button" class="btn btn-secondary" onclick="window.history.back()">
          Back
        </button>
        <button type="submit" class="btn btn-primary">
          Save & Continue
        </button>
      </div>
    </form>
  </div>
</body>
</html>
